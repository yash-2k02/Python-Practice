# Tuple (indexed, immutable)

# 1. count(): Returns the number of times a specified element appears in the list.
# 2. index(): Returns the index of the first occurrence of a specified element.


tup = (10, 20, 30, 20, 40, 70, 20)

print(tup.count(20))
print(tup.index(30))